1.Задача. Написать любую функцию с любимыми параметрами которая возвращает результат. Название F0
Написать еще одну функцию F11, которая должна вызывать функцию F0,а параметрами для вызова она должна взять из своих параметров.
Написать еще одну функцию F12, КОТОРАЯ вызывает F0 параметры собственные.


2. Написать функцию F0, с любыми параметра и которая возвращает результат
Написать функцию F11, КОТОРАЯ ВЫЗЫВАЕТ ФУНКЦИЮ F0, параметры для вызова придумаю сам
Написать функцию F12,  КОТОРАЯ ВЫЗЫВАЕТ ФУНКЦИЮ F0, параметры для вызова придумаю сам
НАПИСАТЬ ФУНКЦИЮ F2.1, которая вызывает функцию F11
Написать функцию  F22, которая вызывает функцию F11 и F12 что они будут делать решаю сам.
Все функции должны возвращать результат
Написать функции F23, которая вызывает функцию только F12


3. Написать функцию F0, с любыми входными параметрами(числовые), и возвращает числовой результат
Написать функцию F11, которая вызывает функции F0, принимает  и возвращает числовой результат.
Название F12, кот которая вызывает функции F0, принимает  и возвращает числовой результат.
Название F21 которая принимает числовые параметры, вызывает функцию F11, ПРОИЗВОДИТ ВЫРАЖЕНИЕ МАТЕМАТИЧЕСКОЕ
над входными параметрами и результатом функции F11,  и возвращаю как результат функции F21
Написать функцию F22, которая на вход принимает числовые параметры, она числовые параметры передает в функцию F11 И F22
для вызова, над результатами функции F12 И F11 ВЫПОЛНЯЕТ математическое выражение и возвращает как результат
Написать F23 которая принимает числовые параметры, вызывает функцию F12, ПРОИЗВОДИТ ВЫРАЖЕНИЕ МАТЕМАТИЧЕСКОЕ
над входными параметрами и результатом функции F12,  и возвращаю как результат функции F23  
Написать F3 которая вызывает F23 F22 F21 произвоодит математическое выражение над результатами вызовов этих функций
и print в консоль
Вызвать функц F3 из корневой программы. 

